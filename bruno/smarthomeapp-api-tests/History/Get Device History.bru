meta {
  name: Get Device History
  type: http
  seq: 1
}

get {
  url: {{api_base}}/devices/history
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response is array", function() {
    expect(res.body).to.be.an('array');
  });
}
