meta {
  name: Set Light Brightness
  type: http
  seq: 2
}

post {
  url: {{api_base}}/devices/light-1/actions
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "action": "SET_BRIGHTNESS",
    "parameters": [75]
  }
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
