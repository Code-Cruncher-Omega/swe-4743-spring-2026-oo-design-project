meta {
  name: Get Environment Names
  type: http
  seq: 2
}

get {
  url: {{api_base}}/environments
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response is array", function() {
    expect(res.body).to.be.an('array');
  });
}
