meta {
  name: Get Updateable Devices
  type: http
  seq: 3
}

get {
  url: {{api_base}}/devices
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response is array", function() {
    expect(res.body).to.be.an('array');
  });
}
