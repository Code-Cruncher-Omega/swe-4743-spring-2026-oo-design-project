meta {
  name: Delete Device
  type: http
  seq: 2
}

delete {
  url: {{api_base}}/devices/light-1
}

tests {
  test("Status code is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
