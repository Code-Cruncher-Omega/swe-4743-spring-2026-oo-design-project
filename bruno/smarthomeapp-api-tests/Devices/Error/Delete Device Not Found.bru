meta {
  name: Delete Device - Not Found
  type: http
  seq: 3
}

delete {
  url: {{api_base}}/devices/nonexistent-device
}

tests {
  test("Status code is 404", function() {
    expect(res.getStatus()).to.equal(404);
  });
}
