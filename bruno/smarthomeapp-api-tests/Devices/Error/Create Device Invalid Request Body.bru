meta {
  name: Create Device - Invalid Body
  type: http
  seq: 4
}

post {
  url: {{api_base}}/devices
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "",
    "location": "Living Room"
  }
}

tests {
  test("Status code is 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
