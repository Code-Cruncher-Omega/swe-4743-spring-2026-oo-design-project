meta {
  name: Reset All Devices
  type: http
  seq: 2
}

post {
  url: {{api_base}}/devices/reset
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
