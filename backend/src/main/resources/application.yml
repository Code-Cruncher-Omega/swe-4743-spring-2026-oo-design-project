cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}

springdoc:
  swagger-ui:
    path: /swagger
    
spring:
  mvc:
    problemdetails:
      enabled: true

logging:
  level:
    edu.kennesaw.smarthome: INFO

server:
  error:
    include-stacktrace: never
    include-message: never
    include-binding-errors: never